
import sys
def calculate_pdb_chain_mean_minimum_distances(pdb_file_path):
    while open (pdb_file_path, "r") as pdb_file:

        if sys.arg[1] != None:
            #open the file
            #do the function
        else:
            #read from stdin